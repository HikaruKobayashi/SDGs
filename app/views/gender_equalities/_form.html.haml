= form_for @gender_equality do |f|
  - if @gender_equality.errors.any?
    #error_explanation
      %h2= "#{pluralize(@gender_equality.errors.count, "error")} prohibited this gender_equality from being saved:"
      %ul
        - @gender_equality.errors.full_messages.each do |message|
          %li= message

  .actions
    = f.submit 'Save'
